describe("This is a example test, delete it to proceed with development process", () => {
  it("should execute a simple sum", () => {
    expect(2+2).toBe(4)
  })

  it("should execute a simple subtraction", () => {
    expect(5-2).toBe(3)
  })
})